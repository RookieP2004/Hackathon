{
  "name": "@aegis-ai/design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React component library per UI_UX_SPECIFICATION.md §0.2 / §7.2 — the design-token-driven components (SeverityChip, RiskGauge, EvidenceDrawer, ...) reused across every role-based screen.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/react": "^18.3.12"
  }
}
