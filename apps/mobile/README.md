# Mobile (Maintenance Technician App) — Reserved

`ARCHITECTURE.md` §23.2 reserves this directory for the mobile-first Maintenance work-order experience (`UI_UX_SPECIFICATION.md` §7's mobile behavior, Tasha's persona in `ARCHITECTURE.md` §4.5).

**Not scaffolded in this pass** — no mobile framework (React Native, Expo, etc.) was specified in the initialization requirements this scaffold was built against. Adding it is a follow-up decision, not an oversight: it needs its own explicit stack choice before scaffolding, the same way the web app's stack was explicitly specified before `apps/web` was built out.
