# Terraform — Reserved

Per `ARCHITECTURE.md` §25.2, cloud infrastructure (managed Kafka, managed Postgres, Kubernetes clusters) is defined here as infrastructure-as-code once a cloud deployment target is chosen. Not populated in this scaffold — local development requires no cloud resources at all.
