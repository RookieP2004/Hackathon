SQL files placed here run automatically, in filename order, the first time the `postgres` container initializes an empty data volume (standard `timescaledb`/`postgres` image behavior).

Empty in this scaffold. `DATABASE_SCHEMA.md`'s migrations land here starting with `DEVELOPMENT_ROADMAP.md` M9 (`001_extensions.sql`).
