# Minimal local-dev MQTT broker config (ARCHITECTURE.md §17.2's MQTT protocol adapter target).
# Anonymous access is fine for local dev only — never use this config beyond a local machine.
listener 1883
allow_anonymous true
persistence false
