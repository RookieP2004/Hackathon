# Kubernetes Manifests — Reserved

Per `ARCHITECTURE.md` §24.2, Helm charts land here once the system moves beyond `docker-compose`-based local/demo deployment toward the staging/production/on-prem-air-gapped tiers described in that section. Not populated in this scaffold — local development is fully served by `infra/docker-compose/`.
